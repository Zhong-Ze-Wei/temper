# 0017 · v0.1.1 人话契约与 AI 工作文件

日期：2026-08-27 ｜ 版本关系：v0.1.0 → v0.1.1（小步进，行为契约新增）

## 目的（改动依据）

用户三个明确要求 + 一项实测发现：

1. 人话导览内置 skill，吸收 humanizer-zh 的思考方式（用户要求）
2. 采纳三条"用户整不整得明白"分析建议：首次预告、先送一小步价值、用户面导览（用户确认"三个建议不错"）
3. 新增"项目工作文件"：一次性生成、别人长期用的 AI 面向 flow 载体（用户要求，参照其 CMES-AI 资讯demo 真实文件形态）
4. 纪律 1"落盘"语义澄清（五场景实测 3/5 暴露的缺陷，state.pending 已登记）

## 改动清单

| 改动 | 位置 | 内容 |
| --- | --- | --- |
| 人话契约（新章节） | 证据诚实之后 | 先结论后理由；具体压倒抽象；不夸大意义/模糊归因/三点排比/粗体标题列表；内部概念首现带白话解释；"讲人话"触发立即重说不辩解 |
| 首次三件套 → 四件套 | 首次进入 | 新增【预告】（会动哪些目录、每轮收到什么、何时需要你） |
| 先送一小步，再要材料 | 首次进入 | 要案例/裁决前先用现有材料跑一版给暂定结果——固定顺序，非风格建议 |
| 项目工作文件（新章节） | 会话恢复之后 | A/B/C 落地结构时生成 AGENTS.md（五节固定：项目在干嘛+用户导览 / 目录职责 / AI 执行前必读 / 本项目的循环（真实路径图）/ 当前契约与已知差异）；不覆盖已有文件；属当前权威区 |
| 会话恢复入口 | 会话恢复 | 第①步改为"项目工作文件（如有）" |
| 纪律 1 语义澄清 | 三条纪律 | 落盘＝写文件即算；授权范围内默认顺手 git 提交，不拿"要不要提交"打扰用户 |
| 模板资产 | 沉淀/AI工作文件模板.md | 五节模板 + 双路线 mermaid（参照用户真实文件：迭代loop_共性模型、评测闭环_真实流程图、资讯demo README） |

版本说明同步：新增"代数以 迭代记录/ 目录为准，commit 前缀只是提示"（修 0016 提交标签 test(0017) 与目录数的错位）。

## 设计取舍

- 模板放仓库 沉淀/ 而非部署包：部署副本必须自包含，SKILL.md 内不引用仓库内路径；五节规则在 SKILL.md 里已完整可执行，模板是维护者的参考资产。
- "先送一小步"升为契约：A 场景实测中它"碰巧做到"且效果好（先跑 12 条给暂定结果再要裁决），从行为观察固化为顺序规则。

## 证据边界

- 本版为契约新增，逻辑自洽；尚未对 v0.1.1 部署版重跑场景实测（夹具已恢复初始态可复跑）。
- 人话契约的效果（用户体感）只有真实用户能验证，第 6 层仍为 0。
