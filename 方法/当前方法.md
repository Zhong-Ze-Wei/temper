# 当前方法

> 本文件是"当前工作方法"的指针与约定，不是方法正文。方法正文权威在安装位置的 skill 资产。

## 当前工作方法指针（本轮不动）

- **权威方法本体**：`/Users/zhong/.agents/skills/temper/`（canonical）
  - `SKILL.md`（主入口，hash `f6fa3a1b…`）
  - `references/`（4 个参考文件，hash 见 `项目定义/当前定义.md`）
  - `agents/openai.yaml`（Agent 界面）
- 本轮（第一代）**只建迭代结构，不修改 SKILL.md 本体**。

## 目标方向（候选，待契约评审）

愿景基线对照（`评测/愿景基线对照.md`）表明：temper 真实差距集中在**用户体验层** ——
把方法论藏起来、带着用户做、progressive disclosure。方向级改动（改变用户可见行为）大概率是**契约级变化**，
需要 Epoch Review，不能当普通一代处理。

## 将来如何发布（约定，本轮不执行）

```
改方法（未来某代）
  → 组装发布快照目录（发布包/ 或 skill/，含 SKILL.md + references/ + agents/）
  → cskills publish <含SKILL.md的目录>
  → 远端 registry（https://skills.gz.cvte.cn）
  → cskills sync temper / cskills add temper
  → 拉回 /Users/zhong/.agents/skills/temper（canonical）
  → symlink ~/.claude/skills/temper 自动生效
  → 校验 .cskills-meta.json fileHashes 与项目内记录一致
  → 更新 .temper/state.json versions + 写运行记录
```

**约定**：
- 发布快照目录（`发布包/` 或 `skill/`）留到**首次改 SKILL.md 的代**再建（避免空目录）。届时以仓库内快照为**权威可编辑副本**，canonical 视为**部署目标**（沿用 reg-news-skill 的 `skill/` → workspace 同步先例）。
- `cskills sync --publish-only` 在本机 CLI 5.9.3 报"未知选项"，标记**待验证**；发布链以 `publish [dir]` + `sync`/`add` 为准。
- 发布是外发动作（写入远端 registry），必须经用户确认后执行，不自动发布。
