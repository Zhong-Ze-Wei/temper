---
name: temper
description: Use when 用户想改进一个结果不稳定的 AI 任务、多版迭代后说不清哪版更好、要把模糊想法做成可交付方法，或项目版本与文档状态开始失控；也适用于口径未定的数据分析、「怎样做对」需要靠真实反馈逐渐形成的流程 AI 化任务。不用于一次性脚本、确定性转换或普通对话。
---

# 淬炼 temper

把一个模糊或不稳定的 AI 任务，变成**可比较、可追溯、持续改进**的工作系统。

## 对用户的承诺

用户不需要懂任何方法论。他说出想改善的任务，然后本 Skill 负责：

1. **给判断**——任务现在什么状态、缺什么、值不值得建循环；
2. **带节奏**——每一代由 AI 跑、比、给证据，用户只在拍板点出现；
3. **给闭环**——每轮固定交付「结果 · 差异 · 建议 · 待确认」四样东西。

用户典型的开局一句话：

- 「这是我现在的项目，帮我看看怎么把结果弄稳定」
- 「这任务跑了几个版本了，说不清哪版好」
- 「我有个想法，还不知道怎么做成能交付的方法」

## 开场行为：第一轮回应的固定形状

被调用后的第一轮回应，按顺序包含以下三块。不堆背景、不倒概念：

```
【判断】任务当前阶段与状态 + 一句为什么。
        必须引用项目里真实的文件 / 运行 / 案例，不空谈框架。
【计划】接下来 2–3 步，每步说清产出什么。
【需要你】最多 2 样东西，每样带默认选项；用户回「行」即走，回改即调。
```

用户只要分析、解释、评审：给完判断就停，全程只读。
用户明确要建结构或继续迭代：走下面的主循环。

## 主循环：一轮 = 跑一代 + 拍一次板

```
你说出任务（一句话就够）
   │
   ▼
① 读懂项目（只读：事实源、方法、评测、运行记录、Git 近况）
   │  产出「判断 + 计划」
   ▼
【你 · 确认方向】 ←── 一轮最多打扰两次，这是第 1 次
   │
   ▼
② 搭刚好够的结构（仅首次；先给「复用 X / 新增 Y」清单，批准后动手）
   │
   ▼
③ 跑一代（运行 + 独立记录，不覆盖上一代）
   │
   ▼
④ 对比上一版（人工可读 Diff · 回归 · 证据，全部链到具体 case）
   │
   ▼
【你 · 拍板】采用 / 回滚 / 按建议再跑一代 ←── 第 2 次
   │
   ▼
⑤ 更新状态页 ──▶ 回到 ③ 进入下一代
   │
   ├─ 目标 / 定义 / 评测尺子实质变化 ──▶ Epoch Review（显式换尺子）
   └─ 任务稳定 ──▶ 收尾：只留状态页 + 已验证经验
```

## 每轮结束的固定输出

1. **这轮做了什么**，产物在哪；
2. **和上一版比**：变好 / 变差 / 看不出来，逐条链接到 case；
3. **我的建议**：采用 / 不采用 / 再跑一代，附理由和证据等级；
4. **需要你做的**：最多 1–2 件，带默认推荐。

向用户解释时用一句人话，不倾倒概念——用到 Epoch、回归、证据分级时先解释再用。

## 什么时候不进循环

一个问题判断：**下次遇到同类任务，这次留下的证据会不会让结果明显更好？不会就直接做完。**

一次性脚本、确定性转换、普通问答、人工几分钟稳定搞定的事 → 不建循环，直接完成。

## 用户需要记住的全部概念（4 个）

1. **三个版本指针**：工作版本（日常演进）≠ 业务校准版本（业务确认过）≠ 生产版本（对外跑着）。更新一个不自动更新另一个。
2. **Epoch**：一段时期冻结「什么叫做对」，只迭代「怎么做」；尺子要变，显式进 Epoch Review，不偷偷换。
3. **证据分层**：便宜证据（结构校验、固定回归）支持日常演进；贵证据（业务打标、生产反馈）在检查点集中用。两层不互相冒充。
4. **人工节点**：方向确认、拍板、业务裁决、高风险判断。人只出现在这些地方，其余尽量不打扰。

其余概念（阶段细分、六种版本关系、证据类型、文档生命周期……）在参考文档里，用到再读；用户全程不需要知道。

## 执行规则

**进入（每次会话开头）**：先只读。找已有事实源——README、AGENTS/CLAUDE、CURRENT 指针、需求与设计文档、方法、评测、运行记录、Git 近期变化——不按固定目录猜。读 [references/assessment.md](references/assessment.md) 判断阶段与强度。信息缺失时，只问会实质改变方案的问题，且必带默认答案。

**建结构（仅首次或用户要求）**：读 [references/project-structure.md](references/project-structure.md)。先把逻辑职责映射到已有路径，再补缺口；动手前先给「复用 X / 新增 Y」清单。不建空目录、不建占位文档。

**定策略（选择会改变做法时）**：读 [references/strategy-selection.md](references/strategy-selection.md)。策略卡只保留适用项；简单任务几句话说清，不为此新建文档。

**每一代**：读 [references/generation-traceability.md](references/generation-traceability.md)。独立成记录，不覆盖上一代；人工可读 Diff 是帮人做决定的，不是重复文件 diff。

**状态变更**：先说清改的是工作 / 校准 / 生产哪个指针。便宜证据可更新工作版本；业务校准必须有 Gold 或明确裁决者；生产必须满足发布与回滚要求。修改触及目标 / 定义 / 评测尺子 → 进 Epoch Review，不做普通文档同步。

**收尾**：更新已有状态页（不建第二份权威文档），让用户一眼看到：阶段、三个指针、最近一代及证据等级、下一步、待人工事项。

## V0.1 边界

不建常驻自动循环、Runtime、Hooks、多 Agent Graph、跨项目面板。除非用户明确要求且授权：不自动多轮修改、不自动 Promotion、不自动改 Gold、不自动更新版本指针。按真实使用中出现的 failure mode 再加能力。
