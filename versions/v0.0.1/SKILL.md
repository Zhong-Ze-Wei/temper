---
name: temper
description: >-
  从模糊想法或尚不稳定的可运行版本出发，理解 AI
  任务现状，并根据反馈成本、风险、版本关系和人工参与方式选择合适的迭代策略，让每一代可比较、可追溯。适用于具有持续学习和改进价值的 AI
  任务；不适用于一次性脚本、确定性转换或普通对话。
author: zhongzewei
version: 0.0.1
---

# 淬炼

把一个模糊想法或尚不稳定的 AI 任务，逐步变成可理解、可比较、可持续改进的工作系统。目标可能在探索中逐渐清楚；不同任务需要不同的反馈节奏、版本关系和人工参与方式，不要套用固定循环。

## 核心原则

- 先理解当前项目，再决定方法。默认先只读检查已有定义、方法、评测、运行记录、README、项目约定和近期变更。
- 复杂度必须解决真实问题。现有文件能承载时更新现有文件；只创建当前循环确实需要的内容，不创建空目录。
- 不变的是诚实和追溯，变化的是策略。每代都说明改动、证据和状态；Gold 频率、人工节点、比较范围和版本晋升方式按任务决定。
- 区分探索与优化。目标仍模糊时，用产物获得学习，不声称某一代已经客观更好；成功标准稳定后，才在同一 Epoch 内比较 Candidate 与 Baseline。
- 同一 Epoch 内冻结“什么叫做好”，迭代“怎么做”。目标、任务定义、评测语义或关键边界实质改变时进入 Epoch Review，不偷偷换尺子。
- 每一代都必须可追溯。至少能从当前契约追到方法、运行、产物、差异、评测和决策。
- 区分工作采用、业务校准和生产发布。便宜证据可以支持可逆的工作版本更新，但不能冒充业务 Gold 或生产验证。
- 版本不一定互相替代。先判断是替换、增量、并行、分流、实验还是契约变化，再决定如何比较和维护指针。
- 保护用户现有结构和修改。逻辑职责比目录名称重要；历史与归档不代表当前规则。

## 工作方式

### 1. 判断用户现在需要什么

将请求理解为以下一种或组合：

- **诊断**：只理解现状并提出建议，不写文件。
- **策略设计**：根据任务特征选择反馈节奏、版本关系、人工节点和状态变化规则。
- **初始化**：为模糊任务或已有 V0 建立最小迭代结构。
- **继续迭代**：运行或修改一代，并按其版本关系与适当参照比较。
- **评审/状态变更**：判断是否采用为工作版本、完成业务校准或进入生产。

用户只要求分析、解释或评审时保持只读。用户明确要求建立、继续或实现迭代时，可以在任务范围内修改项目；首次创建结构前先展示将复用和新增的路径，除非用户已经明确批准初始化。

### 2. 读取项目并判断状态

优先发现已有的 Source of Truth，而不是按固定目录猜测。检查 README、AGENTS/CLAUDE 类约定、CURRENT 指针、需求与设计文档、提示词或方法、评测、样本、运行记录、版本对比和 Git 近期变化。

读取 [assessment.md](references/assessment.md)，判断：

- 任务是否值得建立持续迭代；
- 当前处于 Discovery、Formation、Eval Building、Optimization 还是 Stabilization；
- 目标和反馈是否足够清楚；
- 应使用轻量、标准还是严格控制；
- 哪些子任务处于不同阶段；
- 当前最小下一步是什么。

先给出有证据的判断。信息缺失时，只询问无法从项目发现且会实质改变方案的少量问题。

### 3. 选择迭代策略

任务会持续迭代，或用户询问如何组织迭代时，读取 [strategy-selection.md](references/strategy-selection.md)。基于反馈成本与延迟、风险与可逆性、版本关系、运行频率和人工参与偏好，形成刚好足够的策略。

不要把轻量、标准、严格当作三套固定流程。强度描述控制多少；策略说明怎样迭代。只有会改变执行或记录方式时才输出策略卡。

### 4. 生成刚好足够的结构

创建或调整项目结构前读取 [project-structure.md](references/project-structure.md)。先把目标、状态、方法、评测、运行和经验等逻辑职责映射到已有路径，再只补缺口。

简单或探索任务不要初始化完整标准目录。只有替换式迭代确实需要时才建立 Candidate/Baseline；增量、并行或分流任务按真实关系表达版本。严格任务也只增加已由风险证明必要的控制。

### 5. 运行并记录每一代

开始新的探索轮次、Candidate、版本比较或 Promotion 前读取 [generation-traceability.md](references/generation-traceability.md)。

每一代至少记录：

- 本代目的或待验证假设；
- 使用的定义、方法、输入、评测、模型和关键配置；
- 产物及其位置；
- 相对上一代或 Baseline 的人工可读差异；
- 评测结果、回归、争议和失败；
- 本代决策与下一步。

Discovery 中记录“学到了什么”；正式 Epoch 中记录当前证据实际能支持什么。没有业务 Gold 时可以更新可逆的工作版本，但必须保留“尚未业务校准”的状态。

### 6. 更新当前视图

一轮工作结束后，使用户能快速看到：当前阶段、循环强度、策略、Epoch、版本关系、当前工作版本、最近业务校准版本、生产版本、最近运行、证据等级、人工检查点和下一步。只展示适用字段。更新已有首页或状态页；不要为了展示同一信息再创建第二份权威文档。

## V0.1 边界

本版本不建立常驻自动循环、复杂 Runtime、Hooks、多 Agent Graph、跨项目面板、全面文档治理或外部执行 Backend。除非用户明确要求且任务授权允许，否则不进行自动多轮修改或自动 Promotion。根据真实使用中出现的 failure mode 再增加能力。
