# 0016 · 部署 v0.1.0 与首次实测

日期：2026-08-27 ｜ 版本关系：v0.1.0 不变（本代为部署 + 验证 + 视图维护，无方法改动，不升版本号）

## 做了什么

1. **部署（用户授权"替我部署"）**：`~/.agents/skills/temper` ← 仓库 v0.1.0（SKILL.md + agents/openai.yaml，删除旧 references）。安装位置自此与仓库一致。
2. **结构符合度检查**：按四分区尺子核对仓库。骨架符合（分区表与实际目录对得上、无游离文件）；发现两处未声明项并修正（README 分区表补"评测/（尺子）"与愿景 html 归属，commit 79973aa）。
3. **首次实测（subagent，拟人最小提示词）**：让一个全新 subagent 只加载部署版 SKILL.md，收到一句话——"帮我看看我这个项目，感觉文件有点乱，不知道现在的结构合不合理"，对 /Users/zhong/code/temper 做只读诊断。

## 实测结果：契约 8 项全过

| 契约项 | 结果 |
| --- | --- |
| 会话恢复顺序（权威→过程最新→尺子→状态） | ✅ 严格按序（SKILL.md→0015→评测说明→state.json） |
| 先说"我理解的现状"再动手 | ✅ |
| 只读不改（用户没让改） | ✅ 全程零写入 |
| 首次三件套（判断/计划/需要你） | ✅ |
| 判断引用证据（文件级核对，非泛泛而谈） | ✅ 逐字节 diff、git 记录、行号级确认 |
| 验证与推演分开标注 | ✅ 明确标注哪句是推断 |
| 术语不吓人 | ✅ 概念带白话解释 |
| 不把自举当真实使用证据 | ✅ 主动声明"这次提问算自举，不算那条证据" |

## 实测的新发现（subagent 独立抓到）

1. **部署后三处视图立即过期**（README/版本说明/state 仍写"安装副本 v0.0.1"）——"当前视图漂移"的活体案例，本代已修正。
2. 评测/ 内两份运行记录（评审-迭代复盘、并发审查-设计符合度）放在尺子区，职责混了，应归过程区（待用户确认后动）。
3. 尺子本身过期：评测说明/测试任务集仍按 v0.0.7/0.0.8 口径写（待用户确认后重写，即 T8 的一部分）。
4. cskills-meta 风险：安装位置元数据仍为 0.0.2 旧哈希，`cskills sync` 可能回退（已在 README/版本说明标注"以仓库为权威"）。

## 证据边界

- 本实测是**模拟场景**（subagent 按指令加载 skill），证据层级约第 1-2 层；它自己 correctly 指出这不算第 6 层真实使用证据。
- 第 6 层仍为 0。v0.1.0 的成立性仍待真实任务验证（C 或 D 场景）。

## 下一步

真实任务验证（等用户提供实例：散乱目录或最近用 AI 跑的真实任务）。
