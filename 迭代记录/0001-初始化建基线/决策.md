# 0001-初始化建基线 · 决策

> 第一代：用 temper 方法论迭代 temper 自身的第一步 —— 建结构 + 建评测基线。SKILL.md 本体未动。

## 本代产物

1. **迭代结构**：README / `.temper/state.json` / `项目定义/` / `循环/` / `方法/` / `评测/` / `运行记录/`
2. **评测基线**：`评测/愿景基线对照.md`（8 维对照表 + P0/P1/P2 缺口清单）
3. **状态视图**：`循环/当前状态.md`（镜像 state.json）+ `循环/循环总览.md`（循环小图）
4. **当前契约**：`项目定义/当前定义.md`（SKILL.md 现状 + hash 钉住）

## 基线缺口快照（来自愿景基线对照）

- **P0（用户体验层）**：
  1. 复杂度在后台 —— SKILL.md 直接暴露概念（诊断卡/Discovery/Epoch/Candidate）
  2. 带着用户做 —— 无用户面对话契约，无"每轮交付四样/打扰≤2 次"约束
- **P1**：
  3. 人只处理高价值缺口（正文未表述为交互承诺）
  4. Gate 前置（该不该迭代未成首屏行为）
  5. 注册表/本地版本漂移（独立项）
- **P2**：只问必要问题 / 维度正交 / 每代可追溯 / 不冒充证据等级 —— 已达标

## 决策

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| 迭代结构 | **采用为工作结构** | 符合 temper project-structure 标准形态；路径与 state.json 映射自洽 |
| SKILL.md 本体 | **本轮不改，挂起** | 用户确认"先只建结构"；改动大概率契约级，需先人工裁决 |
| 愿景基线对照表 | **基线待验收**（baseline_pending） | 8 维与优先级待人工确认反映真实意图 |
| 版本指针 | working 指向 `方法/当前方法.md`（指针）；业务校准/生产 = 无 | 方法本体未进入任何正式指针 |
| 发布 | **不执行** | 无方法改动；发布为外发动作需用户确认 |

## 证据与边界

- 本代证据等级：固定基线（愿景对照），**待人工验收**。
- 愿景对照为便宜证据，支撑方向性判断，**不构成**业务校准或生产验证。

## 需要人工裁决（human_attention）

1. 验收愿景基线对照的 8 维与优先级是否反映真实意图。
2. 裁决：SKILL.md 方法改动是否为**契约级变化**（大概率是，需 Epoch Review，明确哪些历史结果仍可比）。
3. 确认：注册表 `0.0.2` 与本地 SKILL.md `0.0.1` 漂移的处理（建议：下一轮以 canonical 为准对齐或显式声明基线版本）。

## 下一步

- 若人工验收通过 → 进入 Epoch 1 契约评审：把"SKILL.md 用户体验层对齐愿景"定义为一代，生成候选方法 → 对照本表评测 → 达标后走发布链。
- 待清理项：回收/确认废纸篓中的设计思想 HTML；修复开始使用 HTML 的断链。
