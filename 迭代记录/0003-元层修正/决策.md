# 0003 · 元层修正 · 决策

> 根据用户反馈修正迭代仓库本身的组织方式。这一代不改 skill 方法内容（候选 0.1.0 仍在等验收），改的是"这个仓库怎么让用户看懂"。

## 用户反馈（5 条，逐条落实）

1. **迭代流程要直观可视** → skill 主体位置、变前/变后、当前版本在 README 顶部状态表说清。
2. **讲人话** → 候选 SKILL.md 用 humanizer-zh 思路重写（去 AI 味）；README 全部口语化。
3. **根目录要直观 loop 图** → README 顶部 + 循环/循环总览.md 都放了 Mermaid 流程图，标当前节点。
4. **README 要完整说明** → 重写为"一眼看懂 / 你现在最需要知道 / 仓库是干嘛的 / 版本与主体 / 目录导航 / 怎么用 temper 迭代它 / 给新人"。
5. **把 skill 拉进目录 + 版本号** → 建 `skill/temper/` 权威副本（含 VERSION.md），唯一版本号 = SKILL.md frontmatter version；明确"改这里、别改安装位置"。

## 结构变化

| 项 | 之前 | 现在 |
| --- | --- | --- |
| skill 主体 | 只在 `~/.agents/skills/temper`（不可见、会被覆盖） | `skill/temper/`（权威副本，可见，将来发布源） |
| 版本可分辨 | frontmatter 0.0.1 vs meta 0.0.2 混淆 | VERSION.md 统一说明；SKILL.md frontmatter 唯一权威 |
| README | 结构骨架 | 完整导航 + 状态块 + loop 图 + 人话说明 |
| 候选 SKILL.md | 结构对但 AI 味 | humanizer 重写（删排比、破公式、加观点、口语化） |

## 决策

| 决策 | 结论 |
| --- | --- |
| `skill/temper/` 是否采纳为权威副本 | **是** —— 项目内唯一可编辑真身；安装位置降级为部署副本 |
| 候选 0.1.0 是否采纳 | **仍未** —— 继续等用户验收 |
| 是否发布 | 否 |

## 待用户验收（不变）

1. 候选 0.1.0 是否符合"用户体验层对齐愿景 + 讲人话"。
2. 采纳后如何同步到安装位置、版本号策略（0.1.0？对齐注册表 0.0.2？）。
3. 是否走发布链（外发，需单独确认）。
