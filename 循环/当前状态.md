# 当前状态

> 人类可读状态视图。机器状态与路径映射见 `.temper/state.json`（本文件的权威依据）。

## 阶段与强度

| 项 | 值 | 说明 |
| --- | --- | --- |
| 主阶段 | **Formation** | 目标可行动，正在形成第一个可观察版本（候选 SKILL.md） |
| 局部阶段 | Eval Building | 评测尺子（愿景基线对照 v0）已建立 |
| 强度 | **标准** | 需要版本比较与证据积累；无生产/不可逆风险 |
| Epoch | **1** | 已开启；尺子 = 愿景基线对照 v0（本轮为契约级变化） |
| 比较状态 | epoch1_frozen | 尺子冻结，进入候选方法评测 |

## Epoch 1 契约

- **目标**：SKILL.md 用户体验层对齐设计愿景（用户面交互契约）。
- **范围**：P0-1 复杂度在后台 / P0-2 带着用户做（对话契约）/ P1-3 人只处理高价值缺口 / P1-4 Gate 前置 / P1-5 注册表漂移（仅记录）。
- **范围外**：方法论核心（阶段/强度/策略判断、证据梯度、版本指针）不动；不建常驻自动循环。

## 策略

- 版本关系：`contract_change`（契约级变化，需 Epoch Review）
- 反馈节奏：`per_generation_observation`
- 人工方式：`review_each_round`

## 版本指针

| 角色 | 当前值 |
| --- | --- |
| 当前工作版本 | `方法/当前方法.md`（指针，指向安装位置 SKILL.md） |
| 候选版本 | `方法/候选方法/`（E1 用户体验层重构，待评测） |
| 最近业务校准版本 | 无 |
| 生产版本 | 无 |

## 最近运行

- **0001-初始化建基线**（2026-08-26）：建立迭代结构 + 愿景基线对照 8 维 + state.json。产物见 `运行记录/0001-初始化建基线/`。
- **0002（进行中）**：E1 候选方法（SKILL.md UX 层重构 + references 分层）自评与人工 diff。

## 需要你（人工待办）

1. **验收**候选方法的人工 diff 与自评（P0/P1 是否消除）。
2. **确认后采纳**到安装位置 `~/.agents/skills/temper`（不自动采纳）。
3. **确认**是否走 cskills 发布链（外发动作，另行确认）。

## 下一步

生成候选方法 → 对照愿景基线自评 + 人工 diff → 交你验收 → 确认后采纳到安装位置。
