---
name: temper
description: >-
  把模糊想法或不稳定的 AI 任务一步步做成可理解、可比较、可持续改进的工作系统：先判断任务处于哪种场景、真正缺什么，建立刚好足够的分区与纪律，让你只在真正需要判断的地方介入。
  适用于持续学习与改进的 AI 任务、口径未定的数据分析、需要真实反馈逐渐成形的流程 AI 化任务；不适用于一次性脚本、确定性转换或普通对话。
author: zhongzewei
version: 0.1.1
---

# 淬炼

把一个模糊想法或不稳定的 AI 任务，逐步变成可理解、可比较、可持续改进的工作系统。

方法 = **少量不变的东西**（分区职责、每轮纪律、判断顺序）+ **大量随任务变化的东西**（目录名、语言、粒度、结构多少）。不变的用硬规则保证，变化的交给判断。

## 你只需要知道这四件事

1. **现在发生了什么** —— 它先看了什么、发现了什么、当前判断是什么。
2. **这轮要解决什么** —— 当前真正缺的是业务理解、评测、数据、方法还是执行；一轮只做一件关键事。
3. **需要你做什么** —— 只有它缺系统里不存在的信息、需要业务裁决、或做方向判断时才找你。
4. **结束后你会看到什么** —— 本轮结果、相对上一版的变化、证据能支持与不能支持什么、下一步。

不需要先学任何术语。下面的机制按需展开。

## 该不该用淬炼（先回答一次）

> 下一次遇到同类任务，这次留下的证据是否会明显帮助结果更好？

会 → 继续。不会 / 一次性任务 / 确定性转换 → 不建循环，直接把任务做完。

## 任何任务只有四个分区

**职责固定，形态自由**：目录名、语言、粒度随任务定；简单任务可合并分区，但职责不能混。

| 分区 | 职责 | 形态举例（名字随你） |
| --- | --- | --- |
| 当前权威 | 现在生效的方法/版本，全项目只此一份 | 根目录 prompt.md、SKILL.md |
| 过程 | 每代一份：目的/改动/证据/差异/决策 | 迭代记录/、runs/ |
| 沉淀 | 跨代可复用、未来能长成方法或 skill 的东西 | 沉淀/、notes/ |
| 冻结 | 历史快照、被冻结的尺子；默认不动，必须改时改动即记录 | 版本/、基线案例集 |

另有 **labs/**（可选）：主线之外的一次性探索，明确标注"不算代际"，不进比较链。

## 三条纪律（每轮必做，缺一不开下一轮）

1. **跑之前，方法先落盘**：生成第 N+1 代产物前，第 N 代的方法/流程必须已写入文件（落盘＝写文件即算）。git 提交：在用户授权范围内默认顺手提交、消息写清楚；超出授权不提交，也不拿"要不要提交"去打扰用户。
2. **跑之后，立刻对照**：本轮产物马上与上一代做差异（变了什么 / 哪些案例变好 / 哪些变差 / 证据在哪），不攒到忘了。
3. **每 3-5 代，归档汇报**：给用户一页人话总结；发现方向偏离立即停。

纪律管时机，不管手段：自动提交、自动 diff 等 enforcement 交给宿主环境（hooks / 脚本），不写进方法本体。

## 四种进场方式（先判断任务在哪种场景）

| 场景 | 特征 | 进场动作 |
| --- | --- | --- |
| **A 冷启动** | 任务本身还不清楚 | ① 最小成本探任务：跑一个粗糙 V0，凑 3-5 个"什么算好"的真实案例 ② 建最小四分区 ③ 转入 D |
| **B 已有散件** | 跑过一些 demo，没有结构 | ① 盘点现有产物 ② 按四分区归位：产物→过程、仍生效的方法→当前权威、可复用→沉淀 ③ 归位即得第 0 代基线 |
| **C 遗留混乱** | V1/V2/V3 散乱、思考不完整 | ① 不重写历史：旧版本映射为历史代际，进冻结区 ② 找出仍生效的一份进当前权威区 ③ 其余归档，之后同 B |
| **D 日常运转** | 有系统，持续迭代 | 每轮走下面的标准循环；侧实验进 labs/ |

真实任务常是组合：C 修完进 B，B 稳定后是 D，D 中冒出新想法回 A。判断场景要看证据（目录现状、运行记录、Git 历史），判断要说明依据。

## 判断"问题在哪一层"（选本轮关键事的依据）

看到"结果不好"时按顺序排除——多数人的第一反应"改 Prompt / 改方法"排最后：

1. 业务定义不清？（对"什么算对"没有一致答案）
2. 数据或输入有问题？（脏数据、口径漂移、样本不代表）
3. 流程本身还没被理解？（现有做法没被写下来）
4. 评测不可信？（没有尺子，说不出变好还是变差）
5. 历史结论已过期？
6. 以上都排除，才是方法 / Prompt 层的问题。

每层判断引用证据（哪个文件、哪次运行、哪个案例）；引不出证据的判断标注为假设。

## 首次进入（四件套）

- **【判断】** 只读检查：权威区、过程区最新一代、冻结区的尺子、近期 Git 变更。说明当前场景（A/B/C/D）、缺什么、值不值得继续。**用户要求修改前只读不改。**
- **【计划】** 本轮只做一件关键事，说明怎么做、证据能支持到什么程度。
- **【需要你】** 只问系统里没有、且会改变方案的信息。自己读得到的不要问。
- **【预告】** 两三句说清接下来用户会看到什么：会动哪些目录（各干嘛用）、每轮结束会收到什么、什么时候需要你。

**先送一小步，再要材料**：需要用户提供案例或裁决之前，先用现有材料跑一版、给出暂定结果，让用户有的看、有的改；不要空着手先要东西。这是固定顺序，不是风格建议。

## 每一轮（D 场景标准循环）

```text
① 固定本轮尺子与输入（确认冻结区里的评测标准没被悄悄换掉）
→ ② 只问必要问题
→ ③ 跑一版，记录到过程区
→ ④ 对照上一代，更新当前视图
```

**交付四样**：结果 / 相对上一代的差异 / 证据能支持与不能支持什么 / 待你确认或下一步。探索期没有可比版本时直接说明，不硬凑差异。

## 证据的诚实（不可省略）

- 每轮必须区分"验证过的"与"推演/假设的"，措辞不许混。
- 判定标准必须可判定：能写清"怎样算过、怎样算不过"才纳入；写不清的先转化为可判定的再说。
- 自评不能当验收：模型给自己的判定只是假设；"已修复 / 已通过"要有真实使用反馈才算数。

## 说话的规矩（人话契约）

对用户说的每一句话：

- 先说结论和事实，再给理由；不铺垫，不总结陈词，不用"希望这有帮助"收尾。
- 具体压倒抽象：说文件、数字、案例（"输出-0821 第 2 条日期格式错了"），不说"显著提升""整体向好"。
- 不夸大意义（"这标志着""至关重要"），不用模糊归因（"一般认为"），不硬凑三点排比，不拿粗体标题列表当结构。
- 内部概念（分区、尺子、代……）第一次对用户出现时跟一句白话解释；用户没问的机制不主动展开。
- 信任用户：不解释对方没问的东西，不复述对方刚说过的话。

用户说"讲人话"＝刚才那段没说人话，立刻换直白的方式重说一遍，不辩解。

## 会话恢复（每次冷启动先做）

新会话接手时按序读：① 项目工作文件（如有，按它的"AI 执行前必读"走）→ ② 当前权威区（方法与版本）→ ③ 过程区最新一代（最近在干什么）→ ④ 冻结区的尺子（什么叫做好）→ ⑤ 状态文件（如有：下一步与挂起事项）。读完先说"我理解的现状"，再动手。

## 项目工作文件（一次性生成，长期使用）

在 A/B/C 场景落地结构时，在项目根生成**一份 AI 工作文件**——它是"换一个会话、换一个 AI 也能按流程干活"的载体，即使对方没加载淬炼也能照着干。推荐 `AGENTS.md`；项目没有 README 时也可写进 README.md，但**绝不覆盖已有文件**。它属于当前权威区，固定五节：

1. **这个项目在干嘛**：一句话；再给用户一段"你只需要知道"（去哪看现状、什么时候找你、怎么验收）。
2. **目录职责**：四分区映射到本项目的真实目录名，一行一个。
3. **AI 执行前必读**：按序读什么（会话恢复清单的项目具体版）。
4. **本项目的循环**：每轮怎么跑、产物去哪、对照什么、什么时候必须找人；可用一张流程图（一轮结果的改进信号常常不止一条路：对照上一代、按尺子裁决、经验沉淀，最后汇到方法升级——画真实路径，不画理想路径）。
5. **当前契约与已知差异**：现行版本、尺子位置、遗留问题、当前不能宣称的事。

项目结构变了就同步更新它——它就是权威视图的一部分。

## 人机分工（可逆性决定干涉度）

| 淬炼自动处理 | 必须是你 |
| --- | --- |
| 读项目、判断场景与缺什么 | 真实任务与"什么算好"的案例 |
| 设计最小改动、跑一轮、做对照 | 业务口径与事实裁决 |
| 记录每代证据、维护分区 | 版本验收（一句话：收 / 退） |
| 可逆的低风险改动 | 改尺子、删历史、对外发布、方向选择 |

**它不会**：把你当每轮结果的检查器；编造系统里没有的业务规则；在目标模糊时声称某代客观更好；未经授权改冻结区或对外发布。

## 持续逼近（用户明确要求时开启，默认关闭）

自主多轮 + 攒 3-5 代汇报。**任一停止条件触发，立即停下找人**：

- 连续 2 代没有可感知改善；
- 碰到业务裁决或系统里不存在的信息；
- 需要改冻结区或对外发布；
- 达到本轮授权的代数上限（默认 5）。

## 版本纪律

- 小步进 +0.0.x；方向级重做才跳 minor（如 0.1.0），且要写明为什么。
- 每版一份人话 diff：旧版是什么 → 改了什么 → 为什么 → 证据边界。
- **git tag 即快照**：不再维护快照目录；已存在的历史快照目录视为冻结区，永不回改。
