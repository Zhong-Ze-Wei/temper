# 发布与同步

> 规矩：**任何同步/发布，先问用户，点头才动**——本文档就是给任何会话看的操作说明，别绕过问询直接同步。
> 每次同步登记一行（版本号 + commit + 日期 + 目标），记录表在文末，脚本自动追加。

## 什么时候触发问询

一个版本满足任一条，就应该问用户"要不要同步"：

- 部署版场景实测通过（新契约 / 回归全过，报告在 `labs/测试夹具/`）
- state.json 无未消化的高危 pending
- 用户口头认可（"这版不错"）

问的时候给选项：全部 / 只本地 / 本地+GitHub / 先不同步，并如实说明验证状态（哪些场景测过、哪些没测）。

## 四个同步目标

| 目标 | 位置 / 命令 | 性质 |
| --- | --- | --- |
| `agents` | `~/.agents/skills/temper` | 本地 agents 技能 |
| `claude` | `~/.claude/skills/temper` | Claude Code 技能 |
| `github` | push 到 `origin`（https://github.com/Zhong-Ze-Wei/temper ） | 外发（公开仓库） |
| `cskills` | `cskills publish .` + `cskills sync` | 外发（注册表） |

## 快速同步

```bash
./scripts/同步.sh agents claude github cskills   # 只传要同步的目标
```

脚本行为：校验工作区干净 → 读 SKILL.md 的 version → 逐目标执行 → 在文末记录表追加一行 → 提醒提交记录行。github / cskills 是外发动作，脚本只在显式传参时执行，**但传参前先过用户**。

已知坑：`~/.agents/skills/temper/.cskills-meta.json` 是注册表旧哈希（0.0.2），`cskills sync` 可能把部署副本拉回旧版——**仓库永远是权威**，被回退就重跑脚本。

## 同步记录

| 日期 | 版本 | commit | 同步到 |
| --- | --- | --- | --- |
