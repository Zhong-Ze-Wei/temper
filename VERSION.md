# 版本

## 当前定义

- **正式版**：`SKILL.md`（根目录）＝ **v0.0.2**
- **当前候选**：`versions/v0.0.3-candidate/`（用户体验层重构，待你验收）
- **唯一版本号**：SKILL.md frontmatter 的 `version` 字段
- **版本策略**：**小步进** —— 每改一点就 +0.0.x（0.0.2 → 0.0.3 → 0.0.4…），不搞大跳。这个仓库是试验田，版本只反映"改到哪了"，不承诺外部发布。
- **同步纪律**：本地仓库只是试验田，**不主动**同步到 GitHub / CSkills / 安装位置。只有你明确说"这版可以了"，才考虑同步。

## 版本时间线

| 版本 | 是什么 | 状态 |
| --- | --- | --- |
| **v0.0.1** | 初版，概念手册形态。 | 历史快照（versions/v0.0.1/） |
| **v0.0.2** | 当前正式版（根 SKILL.md）。内容同 v0.0.1（概念手册），版本号对齐注册表 0.0.2 的既有记录。 | 正式版（= 根 SKILL.md） |
| **v0.0.3-candidate** | 用户体验层重构：用户面四件事、首次三件套、每轮四样交付、Gate 前置、人只处理高价值缺口。 | 候选，待你验收（versions/v0.0.3-candidate/） |

## 版本怎么升

- **任何改动**（行为、文案、机制微调）→ 小步进 +0.0.x
- **"什么叫做好"变了** → 契约级变化，记一次 Epoch Review，版本仍小步进
- 不搞 0.1.0 这种大跳，除非明确到了"方向级重做"

## 怎么发一版

```text
改 versions/<新版本>/ 里的 SKILL.md（或根 SKILL.md）
  → 验收
  → 同步到安装位置 ~/.agents/skills/temper
  → cskills publish <含 SKILL.md 的目录>  （发到注册表）
  → cskills sync                        （拉回，保持两边一致）
```

发布是**外发动作**，必须你点头才做。

## 两个"temper"别搞混

| | 位置 | 角色 | 能不能直接改 |
| --- | --- | --- | --- |
| 权威副本 | 根 `SKILL.md` + `versions/` | 项目里被迭代的工件 | **能** |
| 部署副本 | `~/.agents/skills/temper` | 各会话实际加载 | **别改**（会被 cskills 覆盖） |
