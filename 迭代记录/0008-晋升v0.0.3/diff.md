# 0008 · 晋升 v0.0.3 为正式版

## 决策

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| v0.0.3 晋升为根 SKILL.md | **是** | A/B/C 测试全通过；用户面重构 + 工作台化 + 三条原则沉淀全部落地 |
| 同步安装位置 `~/.agents/skills/temper` | **否** | 用户选择"只仓库内晋升"；试验田纪律：不主动覆盖全局 |
| 发布注册表 | **否** | 外发动作，需单独确认 |
| 版本号 | 正式版 = **0.0.3**（小步进） | 候选目录 `v0.0.3-候选` 改名为 `v0.0.3`（已采纳快照） |

## 晋升做了什么

- 根 `SKILL.md` ← 候选 v0.0.3（version: 0.0.3）
- 根 `references/` ← 候选 5 个文件（新增 interaction-contract.md + 三条沉淀原则的 project-structure.md）
- `agents/openai.yaml` ← 候选
- 候选目录 `v0.0.3-候选` → `v0.0.3`（已采纳快照，与根 SKILL.md 一致）
- 更新 `版本/版本说明.md`、`README.md`、`.temper/state.json`：v0.0.3 = 当前正式版，无当前候选

## 验证

- 根 SKILL.md frontmatter version = 0.0.3
- references/ 含 5 个文件（含 interaction-contract）
- 安装位置 hash 未变（确认未同步）

## 下一步（建议）

- 开启下一轮 0.0.4 迭代前，先用测试集 A/B/C 对**新正式版**做一遍回归（之前测的是候选版，晋升后路径变了，值得复验）。
- 可以沉淀更多真实使用经验（例如：版本说明作为 diff 功能已被证明有效，是否要把它固化成 temper 的强制要求）。
