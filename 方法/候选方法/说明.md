# 候选方法 E1 · 说明

> 本目录是 Epoch 1 的候选方法（`candidate-e1-ux-contract`），**尚未采纳**。候选与当前工作版本（安装位置 `~/.agents/skills/temper`）分开，决策前不原地覆盖。评测与人工 diff 见 `运行记录/0002-候选E1用户体验层重构/`。

## 版本关系

- **契约级变化**（`contract_change`）：改的是用户可见的产品行为（SKILL.md 交互层），需 Epoch Review，尺子 = `评测/愿景基线对照.md` v0。
- 方法本体（references 机制面）基本不动，仅新增 1 个交互契约参考 + SKILL.md 重构。

## 候选相对现状改了什么（摘要）

| 项 | 现状 | 候选 |
| --- | --- | --- |
| SKILL.md 结构 | 6 步工作方式 + 核心原则（概念导向） | 用户面四件事 + 首次三件套 + 每轮四样交付（行动导向） |
| 概念暴露 | 首屏直接输出诊断卡（阶段/强度/策略/版本关系） | 概念沉到"内部机制（按需展开）" |
| 用户面对话契约 | 无 | 新增 `references/interaction-contract.md`（首次三件套、每轮四样、打扰≤2 次、渐进披露） |
| Gate 前置 | 适用性 Gate 只在 assessment.md | 首屏行为"该不该用淬炼" |
| 人只处理高价值缺口 | references 有支撑，正文未表述 | 职责边界表"淬炼自动处理 vs 只在你介入" |
| 版本 | frontmatter 0.0.1 | 0.1.0 |
| 机制面 references | assessment/strategy/project-structure/generation-traceability | 原样保留 + 新增 interaction-contract.md |

## 布局

```text
方法/候选方法/
├── SKILL.md                 # 重构主入口（候选版）
├── openai.yaml              # Agent 界面（复制自现状，未改）
├── 说明.md                  # 本文件
└── references/
    ├── interaction-contract.md          # ★新增：用户面交互契约
    ├── assessment.md                    # 机制面（原样）
    ├── strategy-selection.md            # 机制面（原样）
    ├── project-structure.md             # 机制面（原样）
    └── generation-traceability.md       # 机制面（原样）
```

## 待验收

- 愿景基线 P0×2 / P1×2 是否消除或降级（见运行记录 0002 自评）。
- 用户确认后，候选才采纳到安装位置 `~/.agents/skills/temper`。
