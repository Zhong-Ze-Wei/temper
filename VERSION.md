# 版本

## 当前定义

- **正式版**：`SKILL.md`（根目录，即当前加载到各会话的版本）
- **当前候选**：`versions/v0.1.0-candidate/`（用户体验层重构，待你验收）
- **唯一版本号**：SKILL.md frontmatter 的 `version` 字段
- **注意**：注册表上记的是 `0.0.2`，与正式版 frontmatter `0.0.1` 不一致（历史漂移，见下）。发布前需要对齐。

## 版本时间线

| 版本 | 是什么 | 状态 |
| --- | --- | --- |
| **v0.0.1** | 初版，概念手册形态。装在各会话的当前版本。 | 正式版（= 根 SKILL.md + versions/v0.0.1/） |
| **v0.1.0-candidate** | 用户体验层重构：用户面四件事、首次三件套、每轮四样交付、Gate 前置、人只处理高价值缺口。 | 候选，待你验收（versions/v0.1.0-candidate/） |
| ~~0.0.2（注册表）~~ | 注册表上的版本号，内容与本地 0.0.1 有差异。 | 漂移，待对齐 |

## 版本怎么升

- **行为变化**（用户看到的变了）→ 次版本，如 0.0.1 → 0.1.0
- **文案/机制微调** → 补丁，如 0.0.1 → 0.0.2
- **"什么叫做好"变了** → 契约级变化，升次版本 + 记一次 Epoch Review

## 怎么发一版

```text
改 versions/<新版本>/ 里的 SKILL.md（或根 SKILL.md）
  → 验收
  → 同步到安装位置 ~/.agents/skills/temper
  → cskills publish <含 SKILL.md 的目录>  （发到注册表）
  → cskills sync                        （拉回，保持两边一致）
```

发布是**外发动作**，必须你点头才做。

## 两个"temper"别搞混

| | 位置 | 角色 | 能不能直接改 |
| --- | --- | --- | --- |
| 权威副本 | 根 `SKILL.md` + `versions/` | 项目里被迭代的工件 | **能** |
| 部署副本 | `~/.agents/skills/temper` | 各会话实际加载 | **别改**（会被 cskills 覆盖） |
