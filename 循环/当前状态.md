# 当前状态

> 人类可读状态视图。机器状态与路径映射见 `.temper/state.json`（本文件的权威依据）。

## 阶段与强度

| 项 | 值 | 说明 |
| --- | --- | --- |
| 主阶段 | **Formation** | 目标可行动（愿景清晰），但尚无"可观察版本"可作为比较基线 |
| 局部阶段 | Eval Building | 正在建立评测尺子（愿景基线对照） |
| 强度 | **标准** | 需要版本比较与证据积累；无生产/不可逆风险 |
| Epoch | 无 | 愿景基线验收通过后才进入第一个 Epoch |
| 比较状态 | baseline_pending | 对照表已草拟，待人工验收 |

## 策略

- 版本关系：`exploration`（本轮是初始化，非替换）
- 反馈节奏：`per_generation_observation`（每代观察记录）
- 人工方式：`review_each_round`（每轮复核关键产物）

## 版本指针

| 角色 | 当前值 |
| --- | --- |
| 当前工作版本 | `方法/当前方法.md`（指针，指向安装位置 SKILL.md） |
| 最近业务校准版本 | 无 |
| 生产版本 | 无 |
| 进行中实验 | 无 |

## 最近运行

- **0001-初始化建基线**（2026-08-26）：建立迭代结构 + 愿景基线对照 8 维 + state.json。产物见 `运行记录/0001-初始化建基线/`。
- 证据等级：固定基线（愿景对照），待人工验收后升级为可引用证据。

## 需要你（人工待办）

1. **验收**愿景基线对照的 8 维与优先级是否反映真实意图（`评测/愿景基线对照.md`）。
2. **裁决**：SKILL.md 方法改动是否为契约级变化（大概率是，需 Epoch Review）。
3. **确认**：注册表 version 0.0.2 与本地 SKILL.md version 0.0.1 漂移的处理。

## 下一步

人工验收基线 →（若通过）进入 Epoch 1，启动"SKILL.md 用户体验层对齐愿景"的契约评审 → 生成候选方法 → 对照基线评测。
